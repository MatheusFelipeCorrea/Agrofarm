import DatePickerInput from "./DatePickerInput.jsx";

/** Filtro de data única — mesmo visual e comportamento do formulário. */
export default function DatePickerFilter(props) {
  return <DatePickerInput {...props} />;
}
